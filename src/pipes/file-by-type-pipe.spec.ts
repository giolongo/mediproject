import { FileByTypePipe } from './file-by-type-pipe';

describe('FileByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FileByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
